#!/usr/bin/make -f

build:

binary: binary-arch binary-indep

binary-arch:

binary-indep:
	dh_installdirs etc
	find etc \( -type f -not -path '*/.svn/*' \) -print0 | xargs -0n1 dh_install
	dh_gencontrol
	dh_installdeb
	dh_installdocs
	dh_installchangelogs
	dh_builddeb

clean:
	dh_clean
